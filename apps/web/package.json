{
  "name": "@localairigs/model-context-fit-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "prepack": "tsc -b && vite build"
  },
  "dependencies": {
    "@localairigs/model-context-fit-core": "workspace:*",
    "@localairigs/model-context-fit-canonical-adapter": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "vite": "^6.0.0",
    "vitest": "^3.1.0"
  },
  "license": "MIT"
}
