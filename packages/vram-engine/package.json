{
  "name": "@localairigs/model-context-fit-vram-engine",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist/",
    "LICENSE",
    "THIRD_PARTY_NOTICES.md",
    "!dist/**/*.map"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm generate:version && pnpm generate:data && tsc -b",
    "prepack": "pnpm build",
    "generate:version": "node scripts/generate-version.mjs",
    "generate:data": "node scripts/generate-data.mjs",
    "validate:data": "node scripts/validate-data.mjs"
  },
  "dependencies": {
    "@localairigs/model-context-fit-core": "workspace:*",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "~5.8.0",
    "vitest": "^3.1.0"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  }
}
